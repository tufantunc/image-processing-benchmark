import type { BenchmarkRun } from "../../../../types";

export function renderFooter(run: BenchmarkRun): string {
  return `
<div class="footer">
  Generated by <strong>${run.runtime.name} ${run.runtime.version}</strong> &middot; ${new Date(run.timestamp).toLocaleString()} &middot;
  <a href="https://github.com/anomalyco/image-processing-benchmark" target="_blank" rel="noopener">image-processing-benchmark</a>
</div>`;
}
